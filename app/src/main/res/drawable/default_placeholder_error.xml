<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Lớp nền màu xám nhạt (giống placeholder) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#EFEFEF"/> <!-- Hoặc màu xám bạn thích -->
        </shape>
    </item>

    <!-- Lớp icon lỗi ở giữa -->
    <item
        android:drawable="@drawable/ic_broken_image_error"
        android:gravity="center"
        android:width="48dp"
        android:height="48dp"/>

</layer-list>